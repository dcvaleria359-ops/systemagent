import React from 'react'
import ReactDOM from 'react-dom/client'
import SystemAgent from './SystemAgent.jsx'
ReactDOM.createRoot(document.getElementById('root')).render(<React.StrictMode><SystemAgent /></React.StrictMode>)
